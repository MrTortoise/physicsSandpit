using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace _3dplayground.Physics
{
    /// <summary>
    /// Static helper class to store common functions in. Will allow easy optimisation later if needed.
    /// If we keep them very small they will compile inline .. 32bytes is the limit iirc
    /// </summary>
    static class PhysFunc
    {

    }
}
